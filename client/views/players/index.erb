<h1>Players</h1>
<table>
  <% for player in @players %>
    <tr>
      <td><%= player.name() %></td>
      <td><%="<a href='/players/#{player.id()}'>See player</a>"%></td>
    </tr>
  <% end %>
</table>
