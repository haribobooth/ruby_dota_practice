<h2>Heroes</h2>
<div class="table-wrapper">
  <table>
    <% for hero in @heroes %>
    <tr>
      <td><%= hero.name() %></td>
      <td><a href='/heroes/<%= hero.id() %>'>See hero</a></td>
    </tr>
    <% end %>
  </table>
</div>
